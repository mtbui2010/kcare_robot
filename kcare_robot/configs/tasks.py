# Runtime configs for kcare_robot.
# ConfigManager discovers this module via ROBOT_SKILLS_PKG=kcare_robot.
# Define any module-level dicts (e.g. ARM_CONFIGS) here that should serve
# as the source of defaults for skills. Empty for now — all defaults live
# in robot_agent.skill_configs proxies.
